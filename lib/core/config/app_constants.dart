/// Application constants including version information
class AppConstants {
  /// Current app version from pubspec.yaml
  static const String appVersion = '1.2.1';
  /// Build number
  static const String buildNumber = '4';
  /// Full version string (version+buildNumber)
  static const String fullVersion = '$appVersion+$buildNumber';
}
